name: go
on:
  push:
    branches:
      - development
  pull_request:
    branches:
      - development
      - main

jobs:
  build:
    name: test
    runs-on: ubuntu-latest
    env:
      GO111MODULE: on
      CGO_ENABLED: 1
    steps:
      - uses: actions/checkout@v2
      - name: Setup go
        uses: actions/setup-go@v2
        with:
          go-version: "1.24"

      - name: Install dependencies
        run: go mod download

      - name: Run build
        run: go build -v .

      - name: Run test
        run: go test ./...
